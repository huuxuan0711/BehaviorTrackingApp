<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background -->
    <item>
        <shape>
            <solid android:color="@color/theme_surface_card"/>
            <corners android:radius="@dimen/weekly_row_spacing"/>
        </shape>
    </item>

    <!-- Left border -->
    <item android:width="@dimen/auto_dp_4">
        <shape>
            <solid android:color="@color/theme_accent_primary"/>
        </shape>
    </item>

</layer-list>

